{
  "name": "bank_statement_v1",
  "description": "Placeholder Reducto schema. Replace with a real Reducto schema that extracts transactions[] with {date, description, amount, ...}.",
  "output": {
    "type": "object",
    "properties": {
      "transactions": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "date": { "type": "string" },
            "description": { "type": "string" },
            "amount": { "type": ["number", "string"] },
            "currency": { "type": "string" }
          },
          "required": ["date", "amount"]
        }
      }
    },
    "required": ["transactions"]
  }
}


